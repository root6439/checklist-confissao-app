import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'checklist.confissao.app',
  appName: 'checklist-confissao',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
